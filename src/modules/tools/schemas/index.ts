import { checkStatusSchema } from './check-status.schema';
import { createTicketSchema } from './createTicket.schema';

export const functionSchemas = [checkStatusSchema, createTicketSchema];

export { checkStatusSchema, createTicketSchema };
